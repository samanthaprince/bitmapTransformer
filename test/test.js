'use strict';

var fs = require('fs');
var transformer = require(__dirname + '../transform')
var expect = require('chai').expect;

describe('', function() {
  it('should', function(){
    expect();
  });
});
